# Lessons 004 — GitHub Setup, Security Audit & Repository Organization

## 1. Security Audit Results

### 1a. Credential Scan — Dart Files
Searched all `flutter/lib/**/*.dart` files for:
- `apiKey`, `appId`, `messagingSenderId`, `storageBucket`

**Result: ✅ NO CREDENTIALS FOUND**

No Firebase configuration, API keys, or hardcoded secrets were found in any Dart source file.

### 1b. Credential Scan — Python Files
Searched all `python/*.py` files for:
- `password`, `secret`, `token`, `Bearer`, `sk-`

**Result: ✅ NO CREDENTIALS FOUND**

### 1c. Firebase Configuration Files
- `google-services.json` — **NOT FOUND** anywhere in the project
- `GoogleService-Info.plist` — **NOT FOUND** anywhere in the project
- `firebase_options.dart` — **NOT FOUND** (project does not use Firebase)

**Verdict:** This project has no Firebase integration. No credentials to remove.

### 1d. Large Files / Build Artifacts
| File | Size | Action |
|:-----|:-----|:-------|
| `flutter/app-release.apk` | 95.1 MB | Excluded via `.gitignore` (`*.apk` rule) |
| `flutter/build/` | ~917 MB (4,856 files) | Excluded via `.gitignore` (`flutter/build/` rule) |
| `flutter/.dart_tool/` | Unknown | Excluded via `.gitignore` (`flutter/.dart_tool/` rule) |
| `flutter/.idea/` | Unknown | Excluded via `.gitignore` (`.idea/` rule) |
| `python/__pycache__/` | Not present | Would be excluded if created (`python/__pycache__/` rule) |

## 2. Files Excluded by .gitignore

The root `.gitignore` successfully excluded:
- `flutter/build/` — ~917 MB of build artifacts
- `flutter/.dart_tool/` — Dart tooling cache
- `flutter/.flutter-plugins` — Generated plugin list
- `flutter/.flutter-plugins-dependencies` — Generated plugin dependency graph
- `flutter/.idea/` — IntelliJ/Android Studio project files
- `flutter/app-release.apk` — 95 MB release binary
- `flutter/flutter_app.iml` — IntelliJ module file (excluded by inner `flutter/.gitignore`)

The inner `flutter/.gitignore` (generated by `flutter create`) additionally excludes:
- `*.iml`, `.idea/`, `build/`, `.dart_tool/`, `.pub-cache/`

## 3. Files That Appeared Staged That Should NOT Have Been

**None.** After running `git add .`, the staged file list was verified with:
```powershell
git status | Select-String -Pattern "build/|\.apk|google-services|\.dart_tool|__pycache__"
```
This returned zero matches — all sensitive/large files were properly excluded.

## 4. Firebase Key Analysis

- `google-services.json` — **DOES NOT EXIST** (`Test-Path` returned `False`)
- `GoogleService-Info.plist` — **DOES NOT EXIST**
- `firebase_options.dart` — **DOES NOT EXIST**

This project does not use Firebase at all. The `.gitignore` includes Firebase rules as a preventive measure for future development.

## 5. Final Commit Statistics

```
git show --stat HEAD
```

- **150 files changed**
- **7,421 insertions(+)**
- **0 deletions(-)**
- Commit hash: `5ed8327`
- Commit message: `feat: initial commit — LSC detector MVP (A/B detection)`

Key files included:
- `README.md` — Professional monorepo README with badges and architecture diagram
- `LICENSE` — MIT License
- `.gitignore` — Monorepo-wide exclusion rules
- `.vscode/extensions.json` — Recommended VS Code extensions
- `flutter/lib/**/*.dart` — All 7 Dart source files
- `flutter/assets/models/sign_model.tflite` — Pre-trained TFLite model (25 KB)
- `flutter/assets/models/labels.txt` — Class labels (A, B)
- `python/collect_data.py` — Data collection script
- `python/train_model.py` — Model training + export script
- `python/dataset.csv` — Training data (60 samples)
- `python/sign_model.tflite` — Source-of-truth model
- `python/labels.txt` — Source-of-truth labels
- `docs/lessons_001_setup.md` — Setup lessons
- `docs/lessons_002_camera.md` — Camera integration lessons
- `docs/lessons_003_inference.md` — TFLite inference lessons

## 6. Git Commands — Failures and Fixes

| Command | Result | Fix |
|:--------|:-------|:----|
| `git status` (before init) | "NO GIT REPO YET" | Ran `git init` to initialize |
| `git log --oneline` (before init) | "NO COMMITS YET" | Expected — first commit hadn't been made |
| `git init` | ✅ Success | — |
| `git add .` | ✅ Success (with LF→CRLF warnings) | Warnings are cosmetic on Windows; no fix needed |
| `git commit -m "..."` | ✅ Success | — |
| `git log --oneline` | ✅ Shows `5ed8327` | — |

**No commands failed.** The LF/CRLF warnings from `git add .` are normal on Windows and do not affect functionality.

## 7. Project Structure Changes Made

### Files Moved
- `flutter/lessons_001_setup.md` → `docs/lessons_001_setup.md`
- `flutter/lessons_002_camera.md` → `docs/lessons_002_camera.md`
- `flutter/lessons_003_inference.md` → `docs/lessons_003_inference.md`

### Files Created
| File | Purpose |
|:-----|:--------|
| `README.md` | Professional monorepo README |
| `LICENSE` | MIT License |
| `.gitignore` | Monorepo-wide exclusion rules |
| `.vscode/extensions.json` | VS Code recommended extensions |
| `docs/lessons_004_github.md` | This file |

### Model Files — Already Present in Both Locations
- `python/sign_model.tflite` (25,488 bytes) — ✅ exists
- `python/labels.txt` (4 bytes) — ✅ exists
- `flutter/assets/models/sign_model.tflite` (25,488 bytes) — ✅ exists
- `flutter/assets/models/labels.txt` (4 bytes) — ✅ exists

No copies were needed — both locations already had identical model files.

## 8. Final Project Structure

```
lsc_detector/
├── .gitignore
├── .vscode/
│   └── extensions.json
├── LICENSE
├── README.md
├── docs/
│   ├── lessons_001_setup.md
│   ├── lessons_002_camera.md
│   ├── lessons_003_inference.md
│   └── lessons_004_github.md
├── flutter/
│   ├── .gitignore                  (inner, flutter-generated)
│   ├── .metadata
│   ├── README.md                   (flutter default)
│   ├── analysis_options.yaml
│   ├── android/
│   ├── assets/
│   │   └── models/
│   │       ├── labels.txt
│   │       └── sign_model.tflite
│   ├── ios/
│   ├── lib/
│   │   ├── main.dart
│   │   ├── core/
│   │   │   ├── constants/
│   │   │   │   └── app_constants.dart
│   │   │   └── theme/
│   │   │       └── app_theme.dart
│   │   └── features/
│   │       └── detector/
│   │           ├── detector_screen.dart
│   │           ├── services/
│   │           │   ├── camera_permission_service.dart
│   │           │   └── inference_service.dart
│   │           └── widgets/
│   │               ├── landmark_painter.dart
│   │               └── result_panel.dart
│   ├── linux/
│   ├── macos/
│   ├── pubspec.lock
│   ├── pubspec.yaml
│   ├── web/
│   └── windows/
└── python/
    ├── collect_data.py
    ├── dataset.csv
    ├── labels.txt
    ├── sign_model.tflite
    └── train_model.py
```

### Files NOT committed (excluded by .gitignore):
- `flutter/app-release.apk` (95 MB)
- `flutter/build/` (~917 MB)
- `flutter/.dart_tool/`
- `flutter/.flutter-plugins`
- `flutter/.flutter-plugins-dependencies`
- `flutter/.idea/`
- `flutter/flutter_app.iml`
